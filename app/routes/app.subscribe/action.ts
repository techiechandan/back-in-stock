import prisma from "../../db.server";


export const action = async ({ request }: { request: Request }) => {
  // This is where you would handle form submissions or other actions.
  // For this example, we are not handling any actions.
  return null;
};